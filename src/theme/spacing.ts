/**
 * Spacing tokens — keep in sync with `tailwind.config.js`.
 */
export const spacing = {
  containerPaddingMobile: 16,
  stackGapSm: 8,
  stackGapMd: 16,
  stackGapLg: 24,
  gutter: 16,
} as const;

export const radius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
} as const;
